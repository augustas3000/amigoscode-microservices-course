spring:
  application:
    name: eureka-server
  zipkin:
    base-url: http://localhost:9411
server:
  port: 8761
eureka:
  client:
#    is this client gonna fetch registry information? no, its the server:
    fetch-registry: false
#    since its a server, it wont need register with itself:
    register-with-eureka: false