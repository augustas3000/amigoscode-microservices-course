server:
  port: 8081

logging:
  level:
    root: DEBUG

spring:
  application:
    name: fraud-service
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/fraud
    username: amigoscode
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true